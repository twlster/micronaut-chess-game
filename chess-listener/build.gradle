application {
    mainClass = "example.micronaut.chess.Application"
}
// Extra config.
tasks.test {
//    dependsOn tasks.named("initContainer")
//    doLast {
//        finalizedBy 'stopContainer'
//    }
}

