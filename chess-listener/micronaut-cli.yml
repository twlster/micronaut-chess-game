applicationType: default
defaultPackage: example.micronaut.chess
testFramework: junit
sourceLanguage: java
buildTool: gradle
features: [app-name, data, data-jdbc, graalvm, gradle, h2, http-client-test, java, java-application, jdbc-hikari, junit, kafka, liquibase, logback, lombok, micronaut-aot, micronaut-build, micronaut-http-validation, netty-server, reactor, readme, serialization-jackson, shade, static-resources, testcontainers, validation, yaml, yaml-build]
